SET NOCOUNT ON;
-- The baseline files are generated from the fully migrated schema. Existing
-- installations should continue to use the idempotent scripts in ../Scripts.
-- This marker intentionally performs no destructive or duplicate migration.
PRINT N'Current module schema is included in 01 through 04.';
